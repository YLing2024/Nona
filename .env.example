# Nona 调试环境变量模板
# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会提交）。
# 应用启动时会自动加载项目根目录的 .env（Android/Web 也支持，作为资源打包）。

# 调试服务商 API Key（非 release 构建自动注入调试服务商）
NONA_DEBUG_API_KEY=

# 调试服务商 Base URL（留空时使用默认值）
NONA_DEBUG_BASE_URL=
