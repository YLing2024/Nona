# Changelog

## [1.3.1] - 2026-08-10

### 新增（竞品超越版）
- **Anthropic / Gemini 流式工具调用**：tool_use / functionCall 分块解析、工具结果回传、2 轮循环验证
- **MCP 审批流**：策略模式（always/on_approval/auto）+ 记住并允许 + 60s 超时拒绝 + 网络日志记录
- **本地工具**：fetch（防 SSRF）/ time_info / calculator / clipboard / memory_tool
- **可编辑上下文摘要压缩**：超限时模型生成四段式摘要（目标/事实/偏好/待办），可编辑/展开/清空/增量压缩
- **成本估算与统计看板**：离线价格表（60+ 模型）+ 自定义价格 + 每日 token 柱状图 + 模型/会话排行 + 月度预算
- **知识库 SQLite 化 + 多库**：语义分块、bigram 倒排、Agent 绑定库、旧数据一键迁移
- **混合检索**：OpenAI 兼容 embedding 向量路 + bigram 关键词路 RRF 融合，未配置自动降级
- **记忆系统**：每 10 轮自动提取、同语义去重合并、会话→Agent→全局三级作用域、memory_tool 对齐
- **世界书**：关键词/常驻触发、5 种注入位置、优先级与预算、JSON 导入导出（SillyTavern 风格）
- **全竞品导入**：NextChat / ChatGPT / RikkaHub / Kelivo + 导入向导（嗅探预览→确认→容错报告）
- **API Key 加密存储**：Windows DPAPI / macOS Keychain / Android Keystore / iOS Keychain，启动自动迁移
- **多 Key 轮换**：按服务商独立 LRU、24h 过期、失败冷却
- **余额查询**：JSON 路径表达式 + 5 分钟缓存 + 服务商列表角标
- **图片生成**：OpenAI 兼容 /images/generations + edits，能力表校验
- **图片 OCR**：vision 模型提取文字替换入消息，提示词可编辑

### 修复
- 12 处占位符未插值（HTTP 状态码/二进制大小/页码等）
- 空会话自动标题崩溃（title_generator StateError）
- S3 列表全桶扫描与 size 错位（prefix + XML 标签提取）
- macOS Release 沙盒无法联网（补 network.client entitlement）
- 导入器单条损坏毁掉整个导入（逐条容错 + 失败报告）
- Web 搜索失败无提示（SearchOutcome 区分失败/无结果）
- 同秒备份文件名互覆（毫秒 + 序号）
- Web 端 sqlite3（dart:ffi）编译隔离（条件导入存根）

## [1.3.0] - 2026-08-08

### 新增
- **多协议支持**：OpenAI / Anthropic（Claude）/ Gemini 原生协议，地址自动探测 + 手动指定；Claude 思考块、Gemini thought 分离、系统提示词与多模态格式自动适配
- **MCP 支持**：服务管理（Streamable HTTP / SSE）、工具自动注入、工具调用循环（最多 8 轮）、取消传播
- **Web 搜索**：Bing / DuckDuckGo 免费解析 + Tavily API + SearXNG 自托管，结果注入上下文并标注引用
- **文档多模态**：PDF（pdfrx）/ DOCX / TXT 文本提取随消息发送
- **Prompt 变量**：{cur_date} {model_id} {locale} {timezone} 等发送时展开
- **自定义请求头/请求体**：服务商级配置，发送时合并
- **Agent 长期记忆**：记忆条目注入系统提示词
- **AI 翻译页**：8 种目标语言流式翻译
- **多模型对比**：同一问题并行多模型并排流式对比
- **本地知识库（RAG）**：文本分块存储 + bigram 检索注入
- **云同步**：WebDAV（Basic Auth）/ S3（SigV4）备份上传/列表/恢复/删除
- **服务商分享**：文本导出/导入（含协议与自定义头）
- **消息分支**：重新生成保存旧版本，一键回退
- **SQLite 存储层**：会话/消息表 + bigram 搜索索引 + 自动迁移 + 原子写入
- **tiktoken 双词表**：cl100k / o200k 按模型自动选择
- **模型上下文窗口库**：内置窗口表 + 联网同步，未配置时自动推断裁剪
- **ZIP 备份**：会话 + 服务商 + Agent 打包，导入 Chatbox / Cherry Studio
- **统一网络层**：连接池、自动重试、自动日志
- **动态主题色**、网络日志过滤/导出、TTS 语速/语言、朗读气泡高亮

### 修复
- 会话文件原子写入防损坏
- 设置页入口超出视口导致的测试与布局问题
- 图片/文档附件回滚恢复

## [1.2.0] - 2026-08-07

Nona v1.2.0 全面升级：多模态图片输入、系统 TTS 朗读、AI 自动标题、会话文件化存储、
精确 token 估算、全文搜索、HTML/PDF 导出、主题定制与中英双语界面。

### 新增
- **图片输入（多模态）**：选择本地图片（多选，单张 ≤8MB）或粘贴图片链接/Data URL；
  气泡内缩略图展示；请求按 OpenAI 多模态 `content` 数组发送；token 估算计入图片开销
- **消息 TTS 朗读**：每条消息「朗读/停止」，调用系统 TTS（Windows/macOS/Android/iOS/Web）
- **AI 自动生成标题**：模型配置页「标题生成模型」，首条消息由 AI 生成标题；
  未配置或失败时维持「截取首条消息」逻辑，且不会覆盖手动重命名
- **精确 token 估算**：内置 tiktoken cl100k_base BPE 实现（纯 Dart、离线），
  与 OpenAI 官方输出逐 token 一致；未加载时回退启发式
- **会话全文搜索**：Ctrl+F 打开全屏搜索页，跨全部会话搜索标题与消息，
  关键词高亮、标题命中置顶；点击结果直接跳转定位到对应消息
- **导出格式扩展**：会话可导出 HTML（自包含单文件）与 PDF（内置 Noto Sans SC
  中文字体、自动分页）；纯图片消息以占位文本导出不丢失
- **Markdown 增强**：任务列表复选框、宽表格横向滚动、Mermaid 图渲染
  （流程图/时序图/饼图等，解析失败回退显示源码）
- **主题定制**：预设色板 + 自定义取色器 + OLED 纯黑深色模式，
  强调色实时驱动 M3 色板
- **中英双语界面**：完整 i18n（zh/en ARB），偏好设置可切换语言或跟随系统
- **发送失败保护**：服务商/模型前置校验失败时自动回滚刚发送的消息并恢复输入，
  避免「图片消息已入列却无法重发」
- **PC 端回车发送**：Windows/macOS/Linux/Web 默认 Enter 发送、Shift+Enter 换行

### 修复
- 流式生成期间不再逐 token 全量持久化；会话写入串行化，杜绝并发乱序覆盖
- TTS 引擎完成回调晚于页面销毁时不再崩溃
- 超大图片（>8MB）拒绝添加，防止内存与存储膨胀
- 删除服务商/模型时联动清理「标题生成模型」悬空配置
- 导出不再跳过纯图片消息

## [1.1.0] - 2026-08-06

Nona 从 v1.0.0 升级到 v1.1.0，重点重构消息编辑能力并新增开发者工具。

### 新增
- **消息编辑重构**：用户与 AI 消息均支持独立编辑页编辑（AI 消息可编辑思考内容与回复内容），编辑结果自动进入后续对话上下文
- **回滚到此处**：任意用户消息一键回滚并回填输入框（移除「继续生成」）
- **回复标注**：每条 AI 回复头部标注本次使用的服务商与模型名
- **开发者模式**：关于页新增开发者模式开关，内置开发者选项（连通性测试提示词、模型能力映射表管理）
- **模型能力映射表**：内置静态映射表（离线兜底）+ 联网更新缓存，按模型 id 自动填充「多模态 / 推理」配置；支持手动「从网络更新」与「启动时自动更新」
- **网络日志**：记录应用内全部网络请求并持久化，支持按天分组列表、详情页、全文查看（虚拟化渲染）、JSON 一键格式化、外部编辑器打开与字节大小统计；可配置最大保留条数
- **移动端导出优化**：无系统保存对话框时自动回退写入应用目录

### 修复
- 调试服务商模型能力配置不持久化
- 删除模型后默认模型配置悬空
- 移动端导出崩溃
- 切换会话时不定位到底部
- 思考内容折叠时显示最新 5 行
